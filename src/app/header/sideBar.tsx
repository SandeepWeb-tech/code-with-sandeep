import { Card, Stack, Typography } from "@mui/material"

const SideBar = () => {
    return(
        <>
        <Card style={{ width : '300px', height: 'auto'}}>
            <Typography>Important Info</Typography>
            <Stack>

            </Stack>
        </Card>
        </>
    )
}

export default SideBar;